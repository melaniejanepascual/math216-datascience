1+1
#comment

#R as a calculator
sqrt(10)-5+exp(7)

#create vector of length 1
mj.vector <- 27

#neg num
new.vector <- -500

#create vector of any length
longer.vector <- c(1,2,3,4,5)

#create a matrix
two.by.two <- matrix(c(1,2,3,4), nrow = 2, ncol = 2, byrow = TRUE)

#paste concatenates strings
mj <- 44
paste(c("MJ", "likes", "the", "num", mj), collapse=" ")

mean(longer.vector)


#read in data
new.data <- read.csv()